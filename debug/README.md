# debug

Development-only content that isn't part of the shipped control/toolpathing
code under `src/lcaf/` -- currently just the automated test suite. Kept out
of the repo root so it doesn't crowd the top level next to `src/`, `docs/`,
`configs/`, etc.

- **`tests/`** -- the `pytest` suite. See
  [tests/README.md](tests/README.md) for what each file covers and how to
  run it.
